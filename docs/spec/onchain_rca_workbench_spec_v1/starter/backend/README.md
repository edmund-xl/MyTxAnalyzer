# Backend Starter

This is a minimal scaffold for Codex to expand. It is intentionally incomplete but establishes the required module layout.

Run target:

```bash
cd starter/backend
python -m venv .venv
source .venv/bin/activate
pip install -e .
uvicorn app.main:app --reload
```

